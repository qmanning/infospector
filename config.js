/*
 * Infospector — optional defaults.
 * Dial in the look you want (right-click the background → Appearance), then use
 * "Copy config snippet" and paste it here. These become the defaults for every browser
 * that opens this copy of the tool ("Reset to defaults" returns here too).
 * Per-browser overrides saved with "Save as defaults" win over this file.
 */
// Optional: name/order the pages the search box lists first (it also reads /sitemap.xml and
// learns links from pages you load). Same shape as pages.json.
// window.INFOSPECTOR_PAGES = [{ title: 'Home', path: '/', type: 'page' }];

window.INFOSPECTOR_DEFAULTS = {
  // glass: { blur: 8, sat: 150, light: 0.3, dark: 2, tint: 14, color: '#bbbbbc', backing: 35 },
  // bg: { pattern: 'dots', opacity: 50, patternColor: null, groundColor: null }
};
